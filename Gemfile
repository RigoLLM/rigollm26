source "https://rubygems.org"

# Matches the GitHub Pages build environment (classic "Deploy from a branch").
# Local preview:  bundle install  &&  bundle exec jekyll serve
gem "github-pages", group: :jekyll_plugins

# Windows / JRuby compatibility helpers (harmless elsewhere)
gem "webrick", "~> 1.8"
